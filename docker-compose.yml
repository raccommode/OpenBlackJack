version: "3.9"

services:
  blackjack:
    build: .
    container_name: openblackjack
    ports:
      - "8000:8000"
    volumes:
      - blackjack_data:/app/data
    environment:
      - UVICORN_RELOAD=false
    restart: unless-stopped

volumes:
  blackjack_data:
